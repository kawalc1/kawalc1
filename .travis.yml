sudo: required
language: python
python: 3.7-dev
before_install:
  - echo $SERVICE_ACCOUNT > service-account.json
deploy:
  provider: gae
  keyfile: service-account.json
  project: kawalc1
  on: master
